# nixpacks.toml

[providers]
providers = ["python"]

[nix]
nixPkgs = ["uv", "python312"]

[phases.install]
cmds = ["uv sync --frozen --no-dev"]

[start]
cmd = "uv run alembic upgrade head && uv run cleansheet-api"
